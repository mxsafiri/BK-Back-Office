{
  "name": "@fimco/api-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "//": "Source-export (like @fimco/ui): the frontends transpile this via Next transpilePackages, so no pre-build/dist is needed on Vercel. tsc -b still emits dist for any non-bundler consumer.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b tsconfig.build.json"
  }
}
